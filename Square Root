def squareroot(x):
    root = x
    lastRoot = 0
    while root != lastRoot:
        lastRoot = root
        root =  0.5 * (root + (x / root))
    return root
    
        
num = float(input("Enter a number: "))
print("The square root of", num,"is",squareroot(num))
